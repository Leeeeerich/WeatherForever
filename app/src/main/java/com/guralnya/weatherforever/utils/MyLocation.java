package com.guralnya.weatherforever.utils;

import android.Manifest;
import android.content.Context;
import android.content.pm.PackageManager;
import android.support.v4.app.ActivityCompat;
import android.support.v4.content.ContextCompat;

public class MyLocation {
/*
    private static int REQUEST_CODE_PERMISSION_ACCESS_FINE_LOCATION = 1;
    public int getPermissionStatus(Context context) {
        return ContextCompat.checkSelfPermission(context, Manifest.permission.ACCESS_FINE_LOCATION);
    }

    public boolean hasPermissionGranted(Context context) {
        if (getPermissionStatus(context) == PackageManager.PERMISSION_GRANTED) {
            return true;
        } else {
            ActivityCompat.requestPermissions(
                    context, new String[]{Manifest.permission.ACCESS_FINE_LOCATION},
                    REQUEST_CODE_PERMISSION_ACCESS_FINE_LOCATION);
        }
        return false;
    }
*/

}
